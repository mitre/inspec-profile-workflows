name: Build Profile Archive

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    # Replace WORKFLOW_COMMIT_SHA with a published, full 40-character commit SHA.
    uses: mitre/inspec-profile-workflows/.github/workflows/build.yml@WORKFLOW_COMMIT_SHA
